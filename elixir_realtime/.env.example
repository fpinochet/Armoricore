# Armoricore Elixir Phoenix - Environment Configuration
# Copy this file to .env and fill in your actual credentials
# .env files are gitignored and will not be committed to version control

# Message Bus (NATS JetStream)
MESSAGE_BUS_URL=nats://localhost:4222

# Database (PostgreSQL) - REQUIRED
# Format: postgresql://username:password@host:port/database_name
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/armoricore_realtime_dev

# JWT Secret - REQUIRED for production
# Generate a secure random string for production
# For development, you can use: default-secret-change-in-production
JWT_SECRET=default-secret-change-in-production

# Phoenix Secret Key Base - REQUIRED for production
# Generate with: mix phx.gen.secret
# For development, a default is used in config/dev.exs
# SECRET_KEY_BASE=your-secret-key-base-here

# Phoenix Host
PHX_HOST=localhost
PORT=4000

# Media Engine gRPC (Optional)
# MEDIA_ENGINE_GRPC_URL=http://localhost:50051

# Environment
# MIX_ENV=dev

# Database Pool Size (Optional)
# POOL_SIZE=10
